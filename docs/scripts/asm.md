Assembly Checker
================
This script is used to inspect the assembly output from the C compiler.

Usage
=====
You must first configure the kernel (see [build.md](../build.md)), and `less` must be installed on the system.
Command: `scripts/gen.py <file> <optimization flag>`
